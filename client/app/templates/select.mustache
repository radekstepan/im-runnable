<div class="select {{ #if expanded.value }}expanded{{ /if }}">
    <div class="field">
        <span>{{ #languages }}{{ #if active }}{{ label }}{{ /if }}{{ /languages }}</span>
        {{ #if expanded.value }}
            <div class="icon up-dir"></div>
        {{ else }}
            <div class="icon down-dir"></div>
        {{ /if }}
    </div>
    <div class="dropdown">
        <div class="search">
            <span class="icon search"></span>
            <input class="input" type="text" autocomplete="off" spellcheck="off" />
        </div>
        <ul class="options">
            {{ #languages }}
                <li can-click="select" {{ #if active }}class="active"{{ /if }}>{{ label }}</li>
            {{ /languages }}
        </ul>
    </div>
</div>